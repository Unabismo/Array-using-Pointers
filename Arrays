#include <iostream>
using namespace std;
int main()
{
    int n,i,*p;
    cin>>n;
    int a[50];
    for(i=0;i<n;++i)
      cin>>a[i];
    p=&a[0];
    for(i=0;i<n;++i)
      cout<<*(p+i)<<endl;
    
    return 0;
}
